# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **קרא קודם את [README.md](README.md).** הוא מתאר את המוצר, את הארכיטקטורה, את
> מכשולי iOS ואת הפריסה — ואין טעם לשכפל אותו כאן. הקובץ הזה מוסיף רק את מה
> שנדרש כדי *לעבוד* בריפו: פקודות, אינווריאנטים שנשברים בשקט, ומלכודות שכבר
> עלו בזמן.
> [tavor-gym-spec.md](tavor-gym-spec.md) הוא הבריף המקורי — הקשר היסטורי, לא מפרט חי.

## פקודות

```bash
npm run dev                       # שרת פיתוח (‎http://localhost:5173/tavor-gym/)
npm run verify                    # lint → typecheck → test → build → check:size
npm run deploy                    # פריסה ל-GitHub Pages, כולל אימות שהאתר באמת עודכן
```

**`npm run verify` הוא השער לפני פריסה.** הוא מריץ בדיוק את מה ש-CI מריץ, באותו
סדר. `npm test` לבדו **אינו** מספיק: vitest מדפיס `Errors 1 error` בשורה נפרדת
משורת ה-`Tests`, ויוצא בקוד 1 גם כשכל הבדיקות עברו. גריפ על `FAIL`/`Tests` בלבד
מפספס את זה — וזה כבר חסם פריסה אחרי שש הרצות שהוכרזו "עוברות".

בדיקה בודדת:

```bash
npx vitest run src/freestyle.flow.test.tsx          # קובץ אחד
npx vitest run src/freestyle.flow.test.tsx -t "כיול"  # בדיקה אחת בשם
npm run test:watch                                   # מצב צפייה
```

צילום מסך של האפליקציה החיה: ראה `.claude/skills/app-screenshot/`.

## שכבות

```
db/       Dexie + כל הישויות. types.ts הוא המקור היחיד לטיפוס ישות
domain/   פונקציות טהורות. בלי Dexie ובלי React — כאן יושבת כל האינטליגנציה
state/    Zustand: activeWorkoutStore (האימון הפעיל), builderBasket (הסל)
screens/  מסך לכל מסלול · components/ · hooks/ (העטיפות של ה-API של iOS)
```

הכיוון הוא חד-סטרי: `screens` → `state`/`db` → `domain`. **`domain/` לא מייבא
כלום מהשכבות שמעליו**, וזה מה שמאפשר לכסות את כל ההמלצות, הנפח, השיאים והכיסוי
בבדיקות יחידה בלי DOM.

## אינווריאנטים שנשברים בשקט

מה שאין עליו טיפוס או בדיקה שתתפוס אותו מיד:

- **אין להאזין ל-`activeWorkout` עם `useLiveQuery`.** היא נכתבת אחרי כל נגיעה.
- **אימון פתוח לא נדרס.** `start`/`startWithItems` מחזירים `'busy'` ולא קוראים
  ל-`discard`. הסטים כבר ב-`setLogs` אבל שורת ה-`session` נכתבת רק ב-`finish`,
  ולכן דריסה משאירה סטים יתומים שנספרים בגרפים ובשיאים ואי אפשר למחוק.
- **`perSide` מוכפל בפונקציה אחת בלבד** — `domain/volume.ts`. כל מקום שמציג
  משקל של תרגיל כזה מציג גם "כל צד".
- **סטי חימום נשמרים ולא נספרים** — לא בנפח, לא בשיאים, לא בהמלצות.
- **`bone-600` אינו צבע טקסט.** 3.0:1 מספיק לאייקון או לגבול, לא למילים.
  טקסט משני `bone-500`, ראשי `bone-50`.
- **הבמה בכרטיס האימון היא 196px קבועים** (`ExerciseCard`), עם המצבים
  `work` · `rest` · `rate` · `rir` · `choose`. מקטע חדש נכנס לגיליון — לא לטור,
  ולא בגלילה בתוך הכרטיס.
- **`RestOverlay` הוא הבעלים היחיד של הצליל וההבזק.** הוא נשאר מורכב גם כשהוא
  `collapsed` בדיוק בשביל זה; `InlineRest`/`RestChoose` מריצים `useRestTimer`
  עם `onFinish` ריק. שני בעלים = שני צפצופים.
- **בחירה מתוך `RestChoose` לא נוגעת ב-`restEndsAt`.** זו כל הפואנטה של הפאנל.
- **אימון חופשי הוא `routineId === null`** — זה מה שגורם להיסטוריה ולסיכום
  לקרוא לו "אימון חופשי" בלי שאף מסך יידע עליו. **אבל זה לא מספיק למסך
  האימון:** גם הבונה וגם "בנה לי אימון" פותחים אימון בלי תוכנית ועם תור שנבחר
  מראש, ו-`ActiveWorkout.planned` הוא מה שמפריד ביניהם. בלעדיו המסך הציג
  חמישה תרגילים שנבחרו בסל כאילו אין אף אחד מהם.
- **"הוסף תרגיל" מוביל תמיד ל-`/freestyle`** — רשת השרירים ואז רשימת השריר,
  מכל דלת (כותרת האימון, גיליון התור, מסך המנוחה, המצב הריק). `?add=queue`
  הוא ההבדל היחיד: בלעדיו הבחירה מתחילה את התרגיל מיד, ואיתו היא דוחפת אותו
  לסוף התור ומחזירה לאימון.
- **כל יעד לחיצה ≥44px**, גם כשהוויזואל 28/30/38/40 — דרך פסאודו שקוף
  (`relative … after:absolute after:-inset-y-[…]`), בלי לשנות את הגובה החזותי.

## עברית ו-RTL

- **כל מספר, טווח ושעון עוטפים ב-`dir="ltr"`.** בלי זה `8–12` נקרא כטווח יורד,
  `25×10` הופך ל-`10×25`, ו-`+30` נקרא `30+`. זו טעות שקטה: שני המספרים סבירים.
- מספרים מקבלים `.tnum` כדי שלא יקפצו ברוחב.
- ריבוי בעברית נכתב במפורש — `סט אחד` ולא `1 סטים`, `נשאר 1` ולא `נשארו 1`.
- הערות הקוד והבדיקות בעברית, ומסבירות **למה** ולא מה. אל תכתוב הערה שמתארת
  את השורה שמתחתיה; כתוב את השיקול שיאבד בלעדיה.

## בדיקות

- `src/**/*.test.ts(x)`. בדיקות מסך מרנדרות את `<App />` שלם מול `fake-indexeddb`.
- `resetAll` בכל קובץ מסך חייב לנקות גם את מה שחי **מחוץ למסד**: ה-hash שבכתובת,
  `useWorkout.setState`, ומטמוני המודול (`invalidateHiddenVideos`,
  `invalidateHiddenExercises`, `invalidateVideoPrefs`). `db.delete()` לא נוגע בהם.
- **להמתין לפקד שקיים רק במסך היעד**, לא לטקסט תוכן — שם תרגיל מופיע גם בגיליון
  שנפתח לפניו, ו-`waitFor` עליו נפתר על מצב ביניים.
- **השם הנגיש ב-jsdom הוא שרשור בלי רווחים.** שני `<span>` בתוך כפתור נותנים
  `"אימון חופשיבלי תוכנית…"`, כי אין פריסה שממנה להסיק רווח. השתמש ב-`/^prefix/`.
- `maxWorkers: '50%'` ב-`vite.config.ts` — תיקון לרעידה אמיתית מהרעבת מעבד, לא כוונון.
- `onUnhandledError` שם מסנן **רק** `DatabaseClosedError` (מחיקת המסד בין בדיקות
  דוחה שאילתות שהיו באוויר). כל דחייה אחרת עדיין מפילה את ההרצה — **אין להרחיב**.
- קריאת מסד שנשלחת ואיש לא ממתין לה חייבת `.catch`: "מחק הכל" סוגר את המסד תחת
  מסכים מורכבים.

## פריסה

`npm run deploy` מסרב על עץ עבודה מלוכלך, מריץ את כל השערים, דוחף ל-`gh-pages`,
**וממתין עד שהשרת מגיש את החותמת החדשה**. דחיפה מוצלחת אינה פרסום.
`.github/workflows/ci.yml` בודק ו**לא פורס** — ✅ ירוק שם אומר שהקוד תקין, לא
שהאתר עודכן.

`check:size` נכשל גם על **חריגה מתקרת הצ׳אנק**. אם הגידול מכוון — להעלות את
`LIMITS` ב-`scripts/check-size.mjs` עם הסבר למה, כמו שתי הרשומות שכבר שם.

## חבילות עיצוב (design handoffs)

`design_handoff_*/README.md` הן מפרטי עיצוב שמגיעים כחבילה שלמה. הן **קוד מקור
של עיצוב, לא קוד לייצור** — לא להעתיק מהן, לשחזר בקוד הקיים עם הטוקנים של
`styles/theme.css` (סתירה בין המסמך ל-`theme.css` — `theme.css` מנצח).

בכל חבילה יש סעיף **"שאלות פתוחות — להכריע לפני המימוש"**. להכריע אותן מול
המשתמש לפני שכותבים, לא אחרי — הן משנות מה נשלח. יש בהן גם סעיף "מה כבר קיים —
לא לבנות מחדש"; הוא מדויק, וקריאה שלו חוסכת מנוע שכבר כתוב ומכוסה בבדיקות.
